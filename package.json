{
  "name": "airline-dsl-ai-demo-opencode-cabbage",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["shared", "server", "frontend"],
  "scripts": {
    "test": "npm run test --workspace shared --workspace server --workspace frontend",
    "build": "npm run build --workspace shared --workspace server --workspace frontend",
    "dev": "npm run dev --workspace server"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
