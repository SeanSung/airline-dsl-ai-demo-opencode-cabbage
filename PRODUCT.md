# 产品

<!-- impeccable:product-schema 1 -->

## Platform

web

## 用户

- **主要用户（真实）：** 内部航线规划员/演示者——现场驱动工具的开发者或演示人员。
- **最高频场景：** 在投影大屏前面对领导或客户，用对话方式生成一条沧海校区环绕巡检航线，并一键提交 GBH 模拟飞行验证，全过程约 10 分钟内完成。
- **次要用户（未来，非 MVP）：** 内部操作员日常用其出线；同一界面在重复使用下仍须可用，但演示/投影语境优先。
- **MVP 明确不服务：** 专业测绘单位（RTK/激光雷达/地形跟随）、多机协同/编队、实时飞行控制、外部客户自助使用。

## 产品目的

一个以对话方式（agent 原生）创建与编辑大疆航线的应用。用户无需在传统 B/S 平台逐点打点拖拽，而是用自然语言描述需求；Agent 提取结构化的**意图（Intent）**，通过多轮澄清补齐缺失参数，经确定性的**几何模板**（MVP：环绕 `orbit`）生成几何，产出 **AirlineContent**，校验后在 3D 地图预览，并一键提交 **GBH 模拟飞行平台**验证。

成功标准 = 10 分钟内从对话产出一条可演示、经 GBH 验证可飞的航线，并可证为真实 LLM 生成——以此验证 agent 原生应用方向，并把出线时间从小时级降到分钟级。

## 定位

- **对话式出线，而非手动打点。** 降低门槛：用自然语言表达需求，agent 编排意图提取→生成→校验→执行闭环。
- **与手动航线走同一条验证链路。** 生成的 AirlineContent 与手绘航线一样经过 GBH 模拟飞行验证，可信度不降级。
- **真实 LLM，且诚实降级。** 生成链路由真实 LLM（DeepSeek，密钥由服务端持有）驱动。LLM 不可用时，规则引擎可兜底以保证演示连续，但界面**必须明确标注"非 AI 生成"**——绝不把兜底产物伪装成 AI 生成。
- 公司向 agent 原生运营平台战略转型的首个试点基线。

## 运行语境

- **界面形态：** 三栏工作台（左：历史/会话列表；中：对话；右：Cesium 3D 地图，叠加航线参数卡与 GBH 提交/结果），外加顶部应用条。
- **视口：** 最佳 1920×1080（投影/演示）；可用下限 1366×768（历史栏收起为约 48px 图标窄条 + 覆盖式抽屉）。不支持平板/手机。在所有受支持断点下，Cesium 始终为右侧主区域。
- **生成管线：** 自然语言 → 多轮澄清 → LLM 提取意图 → 规则几何（环绕）→ AirlineContent → 校验 → 地图预览 → 一键提交 GBH（`POST /api/open/routes`）→ 状态（ok / invalid / error）。
- **编辑 = 自然语言重生成。** MVP 的编辑即对话式重生成（"高度改到 100""加拍照""变成两圈"）；不做地图手动拖点编辑。
- **持久化：** SQLite；历史支持查看 / 加载续编 / 再次提交 GBH。
- **默认值：** WGS-84 坐标；沧海校区机巢锚定 `22.531635, 113.935066`；天地图底图（token 由服务端下发，前端绝不硬编码）。
- **Agent 运行时：** `@earendil-works/pi-ai` + `@earendil-works/pi-agent-core`（pi agent toolkit）；shared/server/frontend 全 TypeScript 单栈。
- **流式：** Agent 回复经 SSE 流式输出；界面展示渐进式输出与解析详情。

## 能力与约束

**已确认能力**
- 多轮澄清：生成前仅就缺失的必填参数（半径/高度/速度/动作）反问补齐。
- 结构化意图：区域、形状（MVP 仅环绕）、高度、速度、动作（拍照/悬停/录像）、云台角、返航配置。
- 流式对话，可查看解析详情。
- Cesium 原生地图航线预览；航线参数摘要卡；GBH 提交并展示 ok/invalid/error 结果。
- 会话/航线历史，支持续编与再次提交。
- LLM 不可用时规则兜底，并清晰标注。
- 诚实的错误处理：失败定位到出错字段并在对话中引导修正；绝不伪装成功；错误信息为人类可读文本，不直接展示原始 JSON/堆栈。

**硬约束**
- LLM 为 DeepSeek；密钥仅存于服务端环境变量。前端绝不接触 LLM 密钥，也不硬编码天地图 token。
- 前端栈固定：React 19 + Vite + TypeScript + 原生 Cesium。样式：Tailwind CSS + shadcn/ui（Radix 无样式原语，源码 copy 进仓库），基于自研深色主题 token。不引入第二套 UI 库，不使用 CSS-in-JS。
- 仅深色主题。token 层须承载主题能力，但 MVP 不实现亮色主题或主题切换。
- 现有组件的功能契约与 props 语义（`ChatPanel`、`HistoryPanel`、`RouteMap`、`GbhPanel`/`GBHSubmitBar`、`chatReducer`、`useChatStream`）保持不变，仅重构样式与布局；现有测试须继续通过。
- `frontend/src/**/*.tsx` 中不得出现承载主题值的内联 `style={{}}`（白名单：Cesium 命令式实体图形属性、以及确实无法用 CSS 表达的运行时像素尺寸，逐处注释说明）。
- 不做地图手动拖点；不做测绘级航线；不做多机；不做实时飞行控制；不接入空域审批流程；不做航线版本管理；不让 LLM 直接生成 KMZ/WPML。

**未定产品事实（记录在案，不臆造）**
- 主色精确色值、字体家族、wordmark/组合标识——由设计阶段确定；未提供任何品牌资产。
- 大屏与笔记本下三栏的精确宽度比例及 min/max-width。
- 地图内参数卡/GBH 卡的定位（右上浮卡 vs 底部内嵌面板 vs 侧边贴靠），约束为不得遮挡航线主体。
- GBH 平台实际地址/端口；演示环境是否软渲染（决定底图默认/回退）。

## 品牌承诺

- **显示名称：** 航线编辑 Agent。
- **语言：** 简体中文界面。
- **方向（PRD 锁定，不可重新解读）：** 深色、科技感的"驾驶舱"质感，UI 与 Cesium 3D 地图视觉融合；1920×1080 下达到对外演示级。
- 未提供 logo、锁定主色、官方字体或其他品牌资产；设计阶段可在深色驾驶舱方向内自由选择色板与字体。

## 在手证据

- 产品 PRD：`docs/prd/airline-dsl-edit-agent.md`（Draft v0.1，2026-08-21）。
- 前端重设计 PRD：`docs/prd/frontend-ui-redesign.md`（Draft v1.0，2026-08-24）——锁定三栏工作台、深色 token 体系、shadcn/ui、Tailwind、WCAG 2.1 AA 及验收标准。
- 权威领域术语表：`CONTEXT.md`（意图、几何模板、AirlineContent、GBH、WGS-84、降级策略、三栏工作台、设计 token、shadcn/ui）。
- 决策地图与 ADR：`docs/dev/decision-map.md`、`docs/adr/`。
- 既有实现（重设计的证据/反参考）：`frontend/src/` 中的内联样式 React 组件；`frontend/src/styles/theme.css` 与 `frontend/src/components/ui/` 中的深色 token 基础。
- 真实资产：服务端航线/GBH 链路、SQLite 持久化、pi-agent 集成。不存在任何客户证言、客户名单、基准数据、定价或授权声明——后续工作不得编造。

## 产品原则

1. **演示即产品。** 在 1920×1080 下，状态变化须一眼可读；界面要强化"真实 AI + 真实 3D 航线"的叙事，而不是又一个表单后台。
2. **真实高于戏剧效果。** 流式输出、校验错误、GBH 结果、AI 与兜底的来源都要诚实展示——绝不 mock 成功，也不把规则兜底标注为 AI。
3. **对话即编辑器，而非外挂聊天框。** 每一次生成、澄清、修正都在同一段连续对话中；地图与参数卡反映当前对话状态。
4. **地图是舞台。** Cesium 始终是右侧主区域；面板与卡片不得遮挡航线主体，也不得破坏与深色主题的视觉融合。
5. **一个可复用的设计基座。** Token 是唯一真相源；业务组件组合 `components/ui/` 原语，使未来的几何/动作/历史界面在不新增视觉常量的前提下扩展。

## 无障碍与包容性

- WCAG 2.1 AA 最低对比度：正文 ≥ 4.5:1，大字/UI 组件 ≥ 3:1。适用于两个受支持断点下的全部文字与状态表面。
- 状态不得仅靠颜色编码——loading/success/error/warning 须将颜色与图标/文案配对，使 GBH 结果、降级横幅、生成状态对色弱观众及投影场景均可辨识。
- MVP 在受支持的桌面端之外，不设平板/手机或额外辅助技术目标，但仍遵循标准键盘可操作与语义化。
